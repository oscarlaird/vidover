.venv/
__pycache__/
*.py[cod]

# Large media files — tracked via Git LFS (see .gitattributes).
# Intermediate work files and the original zip archive are local-only.
data/work/
archives/

# Local secrets / credentials — never commit.
.env
.env.*
!.env.example
